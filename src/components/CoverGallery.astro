---
import { getCollection } from "astro:content";
import { Image } from "astro:assets";

const covers = (await getCollection("portadas")).sort(
    (a, b) => (a.data.number ?? Infinity) - (b.data.number ?? Infinity),
);
---

<div class="cover-grid">
    {covers.map((cover) => (
        <figure class="cover-item">
            <Image
                src={cover.data.cover}
                alt={cover.data.number ? `Actuel Marx Intervenciones N°${cover.data.number}${cover.data.title ? " - " + cover.data.title : ""}` : cover.data.title}
                loading="lazy"
                widths={[240, 480]}
                sizes="(min-width: 900px) 240px, 45vw"
            />
            {(cover.data.number || cover.data.title) && (
                <figcaption>
                    {cover.data.number && <span class="cover-number">N°{cover.data.number}</span>}
                    {cover.data.title && <span class="cover-title">{cover.data.title}</span>}
                </figcaption>
            )}
        </figure>
    ))}
</div>

<style>
    .cover-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
        gap: 24px;
        margin-top: 30px;
    }

    .cover-item {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .cover-item img {
        width: 100%;
        aspect-ratio: 3 / 4;
        object-fit: cover;
        border-radius: 4px;
        box-shadow: 0 4px 12px var(--overlay-black-15);
        transition: transform 0.3s ease;
    }

    .cover-item img:hover {
        transform: translateY(-4px);
    }

    figcaption {
        display: flex;
        flex-direction: column;
        font-size: 0.8rem;
        color: var(--color-neutral-700);
        text-align: center;
    }

    .cover-number {
        font-family: "Libre Baskerville", serif;
        font-weight: 700;
        color: var(--color-primary);
    }

    .cover-title {
        text-transform: capitalize;
        color: var(--color-neutral-500);
    }
</style>
