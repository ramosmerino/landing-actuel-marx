---
const slides = [
  {
    number: 30,
    subtitle: 'PRIMER SEMESTRE 2021',
    heading: 'LOS INTELECTUALES: PODER, DOMINACIÓN Y RESISTENCIAS',
    description: 'Disponible en Ediciones LOM y en las principales librerías del país...',
    type: 'book',
    color: '#1a3a5c'
  },
  {
    number: 29,
    subtitle: 'SEGUNDO SEMESTRE 2020',
    heading: 'REBELIÓN POPULAR CHILENA Y CRISIS DEL MODELO DE ACUMULACIÓN CAPITALISTA MUNDIAL',
    description: 'Disponible en Ediciones LOM y en las principales librerías del país',
    type: 'book',
    color: '#2c1810'
  },
  {
    number: 31,
    subtitle: 'SEGUNDO SEMESTRE 2022',
    heading: 'LAS VIOLENCIAS: PRÁCTICAS SOCIALES, EXPERIENCIAS, TEORÍAS',
    description: 'Disponible en Ediciones LOM y en las principales librerías del país',
    type: 'book',
    color: '#4a1a2e'
  },
  {
    text: 'Una mercancía parece ser, a primera vista, una cosa trivial y comprensible de por sí. De su análisis resulta que es un objeto muy complicado, lleno de sutilezas metafísicas y reticencias teológicas...',
    author: 'Karl Marx, El Capital',
    type: 'quote'
  }
];
---

<section class="hero-slider">
  <div class="slider-container">
    <div class="slides">
      {slides.map((slide, index) => (
        <div class={`slide ${index === 0 ? 'active' : ''}`} data-index={index}>
          {slide.type === 'book' ? (
            <>
              <div class="slide-image">
                <div class="book-cover" style={`--book-color: ${slide.color}`}>
                  <div class="book-spine"></div>
                  <div class="book-content">
                    <div class="book-header">
                      <span class="book-logo">Actuel Marx</span>
                      <span class="book-logo-sub">Intervenciones</span>
                    </div>
                    <div class="book-title">{slide.heading}</div>
                    <div class="book-number">N°{slide.number}</div>
                  </div>
                </div>
              </div>
              <div class="slide-content">
                <h2 class="slide-title">
                  ACTUEL MARX <em>INTERVENCIONES</em> N°{slide.number}
                </h2>
                <p class="slide-subtitle">{slide.subtitle}</p>
                <h3 class="slide-heading">{slide.heading}</h3>
                <p class="slide-description">{slide.description}</p>
              </div>
            </>
          ) : (
            <>
              <div class="slide-image quote-image">
                <div class="marx-portrait">
                  <div class="portrait-inner">K.M.</div>
                </div>
              </div>
              <div class="slide-content">
                <blockquote class="slide-quote">
                  <p>{slide.text}</p>
                  <cite>{slide.author}</cite>
                </blockquote>
              </div>
            </>
          )}
        </div>
      ))}
    </div>

    <button class="slider-nav prev" aria-label="Previous slide">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="15 18 9 12 15 6"></polyline>
      </svg>
    </button>
    <button class="slider-nav next" aria-label="Next slide">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="9 18 15 12 9 6"></polyline>
      </svg>
    </button>

    <div class="slider-dots">
      {slides.map((_, index) => (
        <button
          class={`dot ${index === 0 ? 'active' : ''}`}
          data-index={index}
          aria-label={`Go to slide ${index + 1}`}
        ></button>
      ))}
    </div>
  </div>
</section>

<style>
  .hero-slider {
    position: relative;
    background: linear-gradient(135deg, #4a4643 0%, #2e2927 50%, #1a1817 100%);
    overflow: hidden;
  }

  .slider-container {
    position: relative;
    max-width: 1200px;
    margin: 0 auto;
    min-height: 420px;
  }

  .slides {
    position: relative;
    width: 100%;
    height: 420px;
  }

  .slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    padding: 40px 60px;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.5s ease, visibility 0.5s ease;
  }

  .slide.active {
    opacity: 1;
    visibility: visible;
  }

  .slide-image {
    flex: 0 0 40%;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
  }

  /* CSS Book Cover Styles */
  .book-cover {
    width: 200px;
    height: 280px;
    background: var(--book-color, #1a3a5c);
    border-radius: 2px 8px 8px 2px;
    box-shadow:
      0 10px 40px rgba(0, 0, 0, 0.5),
      inset 0 0 30px rgba(0, 0, 0, 0.2);
    position: relative;
    transform: rotate(-5deg);
    transition: transform 0.3s ease;
    overflow: hidden;
  }

  .book-cover:hover {
    transform: rotate(-2deg);
  }

  .book-spine {
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 15px;
    background: linear-gradient(90deg, rgba(0,0,0,0.3) 0%, rgba(0,0,0,0.1) 50%, rgba(255,255,255,0.1) 100%);
  }

  .book-content {
    padding: 25px 20px 20px 30px;
    height: 100%;
    display: flex;
    flex-direction: column;
    color: #fff;
  }

  .book-header {
    display: flex;
    flex-direction: column;
    margin-bottom: 20px;
    border-bottom: 1px solid rgba(255,255,255,0.3);
    padding-bottom: 15px;
  }

  .book-logo {
    font-family: 'Libre Baskerville', serif;
    font-size: 0.9rem;
    font-weight: 700;
    color: #be2e2a;
  }

  .book-logo-sub {
    font-family: 'Libre Baskerville', serif;
    font-size: 0.75rem;
    font-style: italic;
    color: rgba(255,255,255,0.8);
  }

  .book-title {
    font-family: 'Libre Baskerville', serif;
    font-size: 0.85rem;
    line-height: 1.4;
    flex: 1;
    display: flex;
    align-items: center;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .book-number {
    font-family: 'Libre Baskerville', serif;
    font-size: 1.2rem;
    font-weight: 700;
    color: rgba(255,255,255,0.9);
    text-align: right;
  }

  /* Marx Quote Portrait */
  .quote-image {
    flex: 0 0 35%;
  }

  .marx-portrait {
    width: 180px;
    height: 220px;
    background: linear-gradient(145deg, #3a3632 0%, #1a1817 100%);
    border-radius: 5px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
  }

  .portrait-inner {
    font-family: 'Libre Baskerville', serif;
    font-size: 3rem;
    font-weight: 700;
    color: rgba(255,255,255,0.15);
    letter-spacing: -2px;
  }

  .slide-content {
    flex: 1;
    color: #fff;
    padding-left: 40px;
  }

  .slide-title {
    font-family: 'Libre Baskerville', serif;
    font-size: 1.8rem;
    font-weight: 700;
    margin-bottom: 8px;
    color: #fff;
  }

  .slide-title em {
    font-style: italic;
    color: #be2e2a;
  }

  .slide-subtitle {
    font-size: 1rem;
    color: #be2e2a;
    font-weight: 600;
    margin-bottom: 15px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .slide-heading {
    font-family: 'Libre Baskerville', serif;
    font-size: 1.4rem;
    color: #be2e2a;
    line-height: 1.4;
    margin-bottom: 15px;
    font-weight: 400;
  }

  .slide-description {
    font-size: 1rem;
    color: #d4a84a;
    font-weight: 500;
  }

  .slide-quote {
    max-width: 600px;
  }

  .slide-quote p {
    font-family: 'Libre Baskerville', serif;
    font-size: 1.3rem;
    font-style: italic;
    line-height: 1.7;
    color: #fff;
    margin-bottom: 20px;
  }

  .slide-quote cite {
    font-size: 1rem;
    color: #be2e2a;
    font-style: italic;
  }

  .slider-nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(0, 0, 0, 0.4);
    border: none;
    color: #fff;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.3s;
    z-index: 10;
  }

  .slider-nav:hover {
    background: rgba(190, 46, 42, 0.9);
  }

  .slider-nav.prev {
    left: 15px;
  }

  .slider-nav.next {
    right: 15px;
  }

  .slider-dots {
    position: absolute;
    bottom: 25px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 12px;
    z-index: 10;
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.4);
    border: none;
    cursor: pointer;
    transition: background 0.3s, transform 0.3s;
  }

  .dot:hover,
  .dot.active {
    background: #fff;
    transform: scale(1.3);
  }

  @media (max-width: 900px) {
    .slide {
      flex-direction: column;
      padding: 30px 20px;
      height: auto;
      min-height: 550px;
    }

    .slides {
      height: auto;
      min-height: 550px;
    }

    .slide-image {
      flex: 0 0 auto;
    }

    .book-cover {
      width: 160px;
      height: 220px;
    }

    .slide-content {
      padding-left: 0;
      text-align: center;
      padding-top: 25px;
    }

    .slide-title {
      font-size: 1.3rem;
    }

    .slide-heading {
      font-size: 1.1rem;
    }

    .slider-nav {
      width: 40px;
      height: 40px;
    }

    .slider-nav.prev {
      left: 5px;
    }

    .slider-nav.next {
      right: 5px;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const slides = document.querySelectorAll('.slide');
    const dots = document.querySelectorAll('.dot');
    const prevBtn = document.querySelector('.slider-nav.prev');
    const nextBtn = document.querySelector('.slider-nav.next');
    let currentIndex = 0;
    let autoSlideInterval: ReturnType<typeof setInterval>;

    function showSlide(index: number) {
      slides.forEach((slide, i) => {
        slide.classList.toggle('active', i === index);
      });
      dots.forEach((dot, i) => {
        dot.classList.toggle('active', i === index);
      });
      currentIndex = index;
    }

    function nextSlide() {
      const next = (currentIndex + 1) % slides.length;
      showSlide(next);
    }

    function prevSlide() {
      const prev = (currentIndex - 1 + slides.length) % slides.length;
      showSlide(prev);
    }

    function startAutoSlide() {
      autoSlideInterval = setInterval(nextSlide, 5000);
    }

    function stopAutoSlide() {
      clearInterval(autoSlideInterval);
    }

    prevBtn?.addEventListener('click', () => {
      stopAutoSlide();
      prevSlide();
      startAutoSlide();
    });

    nextBtn?.addEventListener('click', () => {
      stopAutoSlide();
      nextSlide();
      startAutoSlide();
    });

    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        stopAutoSlide();
        showSlide(index);
        startAutoSlide();
      });
    });

    startAutoSlide();
  });
</script>
