---
import * as footerInfo from "../content/footer.md";
import { socialLinks } from "../data/social";
import { withBase } from "../lib/url";
const { title, lines } = footerInfo.frontmatter;

function hrefFor(href: string): string {
  return href === "#" ? href : withBase(href);
}

const footerLinks = [
  { label: "INICIO", href: "/" },
  { label: "NUMERO ACTUAL", href: "/numero-actual" },
  { label: "NUMEROS ANTERIORES", href: "/numeros-anteriores" },
  { label: "CONVOCATORIAS", href: "/convocatorias" },
  { label: "SOBRE LA REVISTA", href: "#" },
  {
    label: "ACERCA DE LA REVISTA",
    href: "/acerca-de-la-revista",
    indent: true,
  },
  { label: "COMITÉ EDITORIAL", href: "/comite-editorial", indent: true },
  {
    label: "NORMAS DE PUBLICACIÓN",
    href: "/normas-de-publicacion",
    indent: true,
  },
  { label: "BLOG", href: "#" },
  {
    label: "COLUMNAS DE OPINIÓN",
    href: "/blog/columnas-de-opinion",
    indent: true,
  },
  {
    label: "CUADERNOS TEMÁTICOS",
    href: "/blog/cuadernos-tematicos",
    indent: true,
  },
  { label: "SEPARATAS", href: "/blog/separatas", indent: true },
  { label: "ENCICLOPEDIA", href: "/enciclopedia" },
  { label: "CÁPSULAS / PODCAST", href: "/capsulas-podcast" },
  { label: "NOTICIAS Y EVENTOS", href: "/noticias-y-eventos" },
  { label: "CONTACTANOS", href: "/contactanos" },
];
---

<footer class="footer">
  <div class="footer-main">
    <div class="container">
      <div class="footer-grid">
        <div class="footer-column">
          <h3 class="footer-title">La Revista</h3>
          <ul class="footer-links">
            {
              footerLinks.map((link) => (
                <li class={link.indent ? "indent" : ""}>
                  <a href={hrefFor(link.href)}>{link.label}</a>
                </li>
              ))
            }
          </ul>
        </div>

        <div class="footer-column">
          <h3 class="footer-title">Suscripción</h3>
          <div class="subscription-info">
            <a
              href="https://lom.cl"
              target="_blank"
              rel="noopener noreferrer"
              class="lom-logo-box"
            >
              <div class="lom-styled-logo">
                <span class="lom-text">LOM</span>
                <span class="lom-sub">EDICIONES</span>
              </div>
            </a>
            <p>Edición papel</p>
            <a
              href="https://www.lom.cl"
              target="_blank"
              rel="noopener noreferrer"
              class="lom-link"
            >
              www.lom.cl
            </a>
          </div>
        </div>

        <div class="footer-column">
          <h3 class="footer-title">{title}</h3>
          <div class="info-content">
            {lines.map((line) => <p>{line}</p>)}
          </div>
        </div>

        <div class="footer-column">
          <h3 class="footer-title">Síguenos</h3>
          <ul class="social-links">
            {
              socialLinks.map((social) => (
                <li>
                  <a href={social.href} target="_blank" rel="noopener noreferrer">
                    {social.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </div>
  </div>

  <div class="footer-bottom">
    <div class="container">
      <p>Copyright © 2003-{new Date().getFullYear()} – Actuel Marx/<em>Intervenciones</em></p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-dark-900);
  }

  .footer-main {
    padding: 50px 0;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 40px;
  }

  .social-links {
    list-style: none;
  }

  .social-links li {
    margin-bottom: 8px;
  }

  .social-links a {
    color: var(--color-neutral-300);
    font-size: 0.85rem;
    transition: color 0.3s;
  }

  .social-links a:hover {
    color: var(--color-primary);
  }

  .footer-title {
    font-family: "Libre Baskerville", serif;
    font-size: 1.3rem;
    font-weight: 400;
    color: var(--color-white);
    margin-bottom: 20px;
    padding-bottom: 10px;
    border-bottom: 1px solid var(--overlay-white-20);
  }

  .footer-links {
    list-style: none;
  }

  .footer-links li {
    margin-bottom: 8px;
  }

  .footer-links li.indent {
    padding-left: 15px;
  }

  .footer-links a {
    color: var(--color-neutral-300);
    font-size: 0.85rem;
    transition: color 0.3s;
  }

  .footer-links a:hover {
    color: var(--color-primary);
  }

  .subscription-info {
    color: var(--color-neutral-300);
  }

  .lom-logo-box {
    display: block;
    margin-bottom: 15px;
  }

  .lom-styled-logo {
    width: 80px;
    height: 80px;
    background: var(--color-white);
    border-radius: 4px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 10px;
  }

  .lom-text {
    font-family: "Libre Baskerville", serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-primary);
    line-height: 1;
  }

  .lom-sub {
    font-size: 0.5rem;
    color: var(--color-neutral-800);
    letter-spacing: 1px;
    margin-top: 3px;
  }

  .subscription-info p {
    font-size: 0.9rem;
    margin-bottom: 5px;
  }

  .lom-link {
    color: var(--color-neutral-300);
    font-size: 0.9rem;
    transition: color 0.3s;
  }

  .lom-link:hover {
    color: var(--color-primary);
  }

  .info-content p {
    color: var(--color-neutral-300);
    font-size: 0.9rem;
    margin-bottom: 5px;
    line-height: 1.6;
  }

  .footer-bottom {
    background: var(--color-dark-950);
    padding: 15px 0;
    text-align: center;
  }

  .footer-bottom p {
    color: var(--color-neutral-400);
    font-size: 0.85rem;
  }

  .footer-bottom em {
    font-style: italic;
  }

  @media (max-width: 1024px) {
    .footer-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 900px) {
    .footer-grid {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .footer-column {
      text-align: center;
    }

    .footer-links li.indent {
      padding-left: 0;
    }

    .lom-logo {
      display: flex;
      justify-content: center;
    }
  }
</style>
